# Med Auto Grant lightweight product-entry / OPL handoff provenance

Owner: `Med Auto Grant`
Purpose: `historical_lightweight_product_entry_opl_handoff_provenance`
State: `history`
Machine boundary: 本文是人读历史参考，保留 lightweight product entry、OPL handoff、旧 Hermes Kernel / OPL Runtime Manager / family orchestration wording 的迁移背景。当前 product-entry shell、direct entry、OPL generated/hosted surface、runtime owner 与 grant truth owner 以核心五件套、active gap plan、contracts、schemas、source、CLI/API 行为与 `contracts/runtime-program/current-program.json` 为准；本文不作为当前 product-entry owner、Hermes default provider、Gateway ready 或 hosted runtime claim。

> 历史参考说明：当前 landed route catalog 与 direct-entry truth 已归 `docs/project.md`、`docs/decisions.md` 和 `contracts/runtime-program/current-program.json` 持有。
>
> 当前阅读注记：本文中的 `Hermes Kernel`、`OPL Runtime Manager` 与 `OPL family orchestration surface` 等旧表述，应按当前链路理解为 `OPL stage-led runtime framework with Agent executors as the minimum execution unit -> MAG projection/descriptor -> Med Auto Grant Product Entry / MedAutoGrantDomainEntry`。除非活跃合同显式选择其他 provider，`Codex CLI` 是默认最小执行单元。OPL 可以托管、调度、唤醒、handoff、receipt、retry 和投影 MAG stage，但不持有 MAG grant truth、authoring semantics、quality gates 或 submission-ready export truth。

## 历史主题读法

本文只保留 lightweight product-entry、direct / OPL handoff envelope、早期 `product build-entry` shell、旧 Hermes / Runtime Manager 表述和 route-catalog 形成过程的 provenance。当前 product-entry、status、user-loop、direct-entry、package/export 和 generated/default caller 读法回到：

- `docs/status.md`
- `docs/active/mag-ideal-state-cross-repo-gap-plan.md`
- `docs/active/opl-private-implementation-migration-inventory.md`
- `docs/specs/product-entry-support-record.md`
- `docs/specs/specs_lifecycle_map.md`
- `contracts/runtime-program/current-program.json`
- product-entry manifest、schemas、source、CLI/API behavior、owner receipts 和 typed blockers

| 历史主题 | 本文保留的 provenance | 当前 owner / 阅读动作 |
| --- | --- | --- |
| Operator / agent entry distinction | 历史上把 workspace 准备、检查、gate、调试和 `CLI` / `MedAutoGrantDomainEntry` 调用面分开。 | 当前入口和 runtime owner 读 `docs/status.md`、核心五件套、`current-program.json` 与 product-entry manifest。 |
| Lightweight product-entry shell | 历史 `product build-entry` / direct / `opl-handoff` envelope 曾把 progress、cockpit 和 product entry projection 收到结构化 shell。 | 当前 hand-written product-entry shell 只能按 refs-only adapter / migration input 读取；删除门和 active caller 分类读 private inventory 与 machine contracts。 |
| OPL handoff envelope | 历史最小字段包括 `target_domain_id`、`task_intent`、`entry_mode`、`workspace_locator`、`runtime_session_contract`、`return_surface_contract`，grant 侧补 `workspace_id`、`draft_id`、`funding_call`、`executor_routing_contract`。 | 当前 handoff / route truth 读 product-entry support record、schema/source/tests、runtime-program contracts 和 current route catalogs；本文不作为 envelope schema owner。 |
| Hermes / Runtime Manager wording | 历史文案中的 `Hermes Kernel`、`OPL Runtime Manager`、`OPL family orchestration surface` 表达当时 provider/substrate 迁移背景。 | 当前默认运行驻留是 OPL/Temporal stage-led runtime，`Codex CLI` 是第一公民 stage executor；Hermes 只作为显式 proof / non-default executor lane。 |
| Product maturity boundary | 历史上已经区分 lightweight shell 与成熟最终 UX。 | 当前不能把 product-entry manifest、grouped CLI success、refs-only accounting、provider completion 或 shell existence 写成 mature UX、grant-ready、export-ready、submission-ready 或 production-ready。 |

## No-Resurrection Boundary

- 本文里的 `当前真相`、`目标形态`、`下一步`、`product build-entry`、`Hermes Kernel`、`OPL Runtime Manager` 和 `OPL family orchestration surface` 只按归档时点读取。
- 不恢复旧 `build-product-entry` 作为当前 product-entry schema owner、Gateway / Hermes default runtime owner、compatibility route alias、hosted runtime claim 或 App/workbench maturity proof。
- 不用本文的 historical handoff envelope 断言当前 schema、source、CLI/API behavior、production default caller、OPL replacement readiness、physical delete authority 或 grant/package readiness。
- 若本文中的某条边界仍应长期生效，先折回 product-entry support record、核心五件套、active gap plan、spec lifecycle map、contract/schema/source/test owner 或 retired-surface provenance；不要在本文追加新的当前状态或执行清单。
