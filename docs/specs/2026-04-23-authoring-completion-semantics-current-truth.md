# Authoring Completion Semantics Current Truth

Owner: `Med Auto Grant`
Purpose: `authoring_completion_semantics_active_spec`
State: `active_current_spec`
Machine boundary: 本文是人读 active spec，只冻结 authoring completion semantics。当前产品状态、runtime owner、App/workbench 和 evidence gate 继续归核心五件套、active gap plan、contracts/schema/source 与 `contracts/runtime-program/current-program.json`。
Last reviewed: `2026-06-12`

## Scope

- Status: `landed / current truth`
- Active tranche: `P4.G authoring-quality-first completion semantics alignment`

## Frozen Semantics

- `Med Auto Grant` 的主任务完成语义以正文科学性与 authoring quality 为主，不以单一导出命令替代正文质量判断。
- `package submission-ready` 继续是更严格的本地交付包导出面，承担“冻结且材料完备时的一次性本地导出”职责；该命令不代表 authoring 主任务的唯一完成条件。
- 形式审查项与客观补件项默认进入 `TODO` 与显式唤醒链路，按闭环计划推进；只有当缺口直接破坏科学论证成立性时，才提升为正文 authoring blocker。
- 一旦 `workspace` 已锁定 funder / family 任务边界，当前主线维持同一 funder 语义闭环，不把后续推进写成 opportunistic 跨 funder 切换。

## Projection Alignment

- `current-program`、`mainline-status`、`grant-user-loop`、`product-status` 等 repo-tracked projection 必须共同反映上述语义，不得出现“submission-ready 即主任务唯一完成判据”或“默认跨 funder 重选”的第二真相。
- quality governance、closure dossier 与 autonomy controller 继续用于质量收口与行动排序，但它们服务的是 authoring 质量主线，而不是替代正文科学判断。

## Out of Scope

- 不把本 tranche 解释成外部官网提交流程已自动完成。
- 不引入新的 opportunistic funder 路由策略。
- 不改写已冻结的 runtime substrate owner / domain owner 分层。
