# MAG 私有实现与 OPL 迁移台账

Owner: `Med Auto Grant`
Purpose: `opl_private_implementation_migration_inventory`
State: `active_inventory`
Machine boundary: 本文是 human-readable 迁移治理台账。机器真相继续归 `contracts/`、schemas、CLI/API 行为、product-entry manifest、sidecar export/dispatch、runtime receipts、workspace artifact 与 MAG owner receipt。
Date: `2026-05-22`

## 当前 clean truth

MAG 是 OPL-compatible grant domain agent。OPL Framework 持有通用 provider runtime、queue、attempt ledger、generic transition runner、workspace/source shell、memory locator、artifact/package lifecycle shell、generated wrapper、observability/SLO 和 App/workbench shell。MAG 必须保留 grant truth、fundability / quality / export verdict、package authority、grant strategy memory body 与 accept/reject decision、owner receipt、transition oracle、typed blocker 和 grant-native helper。

本轮 scan 没有发现需要迁入 OPL 的 grant authority；风险集中在 hand-written product-entry / sidecar / CLI / autonomy controller / domain-runtime 命名和大型聚合文件，容易被误读成 MAG 私有平台。当前这些 surface 只能按 direct domain handler、refs-only adapter、minimal authority function、diagnostic、migration input 或 history/tombstone 读取。

2026-05-21 本轮最小清理已把 `product_entry_parts/sidecar.py` 的 TODO/wakeup 投影从历史 Hermes 24h substrate 口径收薄为 `opl_wakeup_contract`：OPL 持有 typed family queue / provider wakeup shell，MAG 只暴露 `open_grant_user_loop` / `product user-loop` refs-only authoring continuation action target、owner receipt / typed blocker / no-regression return shape 和 no private runtime role flags；`status/read`、`user-loop/wakeup`、`notification/receipt`、`autonomy-controller/dry-run` 与 `autonomy-controller/guarded-run` 不再是 sidecar dispatch actions。随后继续把 sidecar export 的 generic projection helpers 拆入 `sidecar_projection.py`，把 shared sidecar constants 拆入 `sidecar_contract.py`，`sidecar.py` 从 804 行降到 683 行并继续保留 active export/dispatch direct path。autonomy-controller 的 domain authority 仍在 `MedAutoGrantDomainEntry` 与 CLI direct path；该改动不声明 OPL generated sidecar 已 default 化，也不迁移 grant truth、quality/export verdict、package authority 或 controller decision。

2026-05-21 manifest shell 收薄 lane 已把 `manifest_builder.py` 中明显 generic 的 product-entry shell / operator loop / family action-stage manifest assembly 拆到 `product_entry_parts/manifest_builder_parts/shell_assembly.py`；`manifest_builder.py` 继续作为 MAG direct manifest 编排层，保留 grant workspace/mainline 读取、route verification、domain memory、receipt、transition oracle、quality/export verdict refs 和 owner authority surfaces。该状态表示 `needs_split_before_migration` 已转为 split/thinned migration input，仍需要 OPL default/generated product-entry/status/workbench caller、direct/hosted parity、owner receipt / typed blocker roundtrip 与 no-forbidden-write 证据后，才能最终退役 MAG 手写 product-entry shell。

2026-05-21 native helper consumption 收薄 lane 已把 `runtime_registration.native_helper_consumption` 从 MAG manifest 中固定 OPL helper 语言 / backing helper id / per-helper proof role 的实现细节，改为 refs-only `index_consumption_policy`、`authority_boundary` 和 index input refs。MAG 仍暴露 workspace/session/artifact/TODO/runtime-health 输入 refs 和 `opl_index_only` 写入边界；helper implementation、helper language/binary、具体 indexer id、runtime/watch owner 继续归 OPL，不由 MAG 声明。该改动不表示 OPL production/default caller、Temporal long-soak、App/workbench consumption 或 grant-ready / quality/export-ready 完成。

2026-05-22 autonomy controller 收薄 lane 已把 `grant_autonomy_controller.py` 从 961 行降到 171 行，只保留 request validation、start/resume resolution、controller-plan governance 和 `run_grant_autonomy_loop` 调用；原大型 for-loop 迁入 `grant_autonomy_loop_shell.py`，并已纳入 `private_functional_surface_policy` 的 `autonomy_controller` source refs。随后本轮继续把 generic report / fail-closed / tranche-history envelope 抽入 `grant_autonomy_loop_parts.py`，`grant_autonomy_loop_shell.py` 从 876 行降到 457 行，只保留 callback lifecycle、budget spending、mainline runner invocation、rollback/reselection flow 与 grant gate 顺序；新 helper 为 143 行 refs/report envelope migration input。该 loop surface 仍是 `opl_framework_migration_candidate`：后续应由 OPL provider/attempt shell 或 generated operator loop 接管；MAG 保留 grant route/budget/blocker policy、typed blocker、owner receipt handoff 与 AI-first review/export verdict refs。该改动是责任拆分和 active caller 收薄，不表示 OPL 已接管 autonomy controller，也不迁移 grant truth、quality/export verdict、package authority 或 controller decision。

2026-05-22 consumer-thinning audit 收薄 lane 已把 `consumer_thinning_audit.py` 中 retired / tombstone legacy surface 清单拆到 `consumer_thinning_audit_parts.py`；主 audit 从 909 行降到 776 行，继续作为 product-entry manifest 与 functional audit read-model direct path。新 helper 只保存 legacy runtime owner、local journal / attempt ledger、domain runtime patch bridge、flat alias 和 repo-owned scheduler daemon 的 tombstone audit 条目，不持有 runtime/action authority。该改动是大型 audit/read-model 文件的自然责任拆分，不表示 OPL audit/default caller 已接管，也不迁移 grant truth、fundability/quality/export verdict、package authority、owner receipt 或 transition oracle。

2026-05-22 consumer-thinning pack 收薄 lane 已把 `consumer_thinning_pack.py` 中 external evidence request pack 与 generated/hosted default caller proof 拆到 `consumer_thinning_pack_evidence.py`；主 pack 从 901 行降到 606 行，继续承载 minimal authority function taxonomy、declarative grant pack compiler input、generated surface handoff 和 bridge exit gate。新 helper 是 OPL generated/default caller evidence shell 的 migration input，只声明 request refs、receipt shape、direct/hosted parity workorder 和 no-forbidden-write boundary；它不实现 OPL runtime/App workbench，不声明 external evidence 已存在，不迁移 fundability/quality/export verdict、package authority、grant strategy memory accept/reject、owner receipt 或 transition oracle。

2026-05-22 functional closure 收薄 lane 已把 `functional_closure.py` 中 physical skeleton follow-through、repo source root anchor、legacy default caller active-path scan 和 retired surface path scan 拆到 `functional_closure_skeleton.py`；主文件从 825 行降到 517 行，继续编排 manifest functional closure surfaces、owner receipt contract、lifecycle guarded apply proof 和 ideal-state closure status。新 helper 是 OPL scaffold/source-layout/read-model projection 的 migration input，只证明 repo source anchor、legacy active-path absence 和 provenance refs；它不签 owner receipt、不执行 lifecycle mutation、不声明 OPL hosted production soak，也不迁移 grant truth、fundability/quality/export verdict、package authority、memory body 或 typed blocker。

## 分类词表

| class | 含义 |
| --- | --- |
| `domain_authority_retained` | MAG 必须保留的 grant truth、AI-first verdict、package/memory/receipt authority 或 grant-native helper。 |
| `opl_framework_migration_candidate` | 当前手写外壳长期应由 OPL generated/hosted surface 或 shared primitive 承担。 |
| `already_thin_adapter` | 已收薄为 refs-only adapter/projection/diagnostic，仍因 direct path 或 evidence caller 暂留。 |
| `needs_split_before_migration` | 同一文件混合 grant authority 与 generic shell，必须先拆 owner 子域。 |

## Inventory

| surface | lines | class | current active caller | 当前实际职责 | 为什么属于该分类 | MAG 必须保留的 authority | 可迁往 OPL 的 generic 子域 | 迁移/退役门槛 | 推荐验证入口 |
| --- | ---: | --- | --- | --- | --- | --- | --- | --- | --- |
| `src/med_autogrant/product_entry_parts/manifest_builder.py` | 724 | `already_thin_adapter` with `opl_framework_migration_candidate` retirement gate | product-entry manifest/status、direct entry、sidecar export、tests/product_entry_cases | MAG direct manifest 编排层：读取 workspace/mainline/route report，组装 domain entry contract、runtime refs、artifact/package/memory projections、receipt/verdict/consumer-thinning surfaces | `needs_split_before_migration` 已完成第一步：generic product-entry shell / operator loop / family action-stage manifest assembly 已拆到 `manifest_builder_parts/shell_assembly.py`；主 builder 仍是 active direct path 和 split/thinned migration input | grant truth refs、fundability/quality/export refs、package authority refs、owner receipt refs、transition oracle refs | generated product-entry/status/workbench manifest shell、App operator projection shell | OPL generated/hosted caller evidence、direct/hosted parity、owner receipt/typed blocker roundtrip、no-forbidden-write、no-active compatibility alias | `tests/product_entry_cases/test_manifest_and_status.py`、`tests/test_opl_family_contract_adoption.py`、`scripts/verify.sh` |
| `src/med_autogrant/product_entry_parts/manifest_builder_parts/shell_assembly.py` | 451 | `opl_framework_migration_candidate` | `manifest_builder.py` | product-entry shell catalog、operator loop actions、family action catalog / stage control plane / transition oracle manifest projection、quickstart/overview/start surfaces | 该模块集中承载可由 OPL generated/hosted caller 和 App operator projection shell 接管的 assembly；当前只是 MAG direct manifest 的拆分输入，不是长期 MAG owner surface | action/stage refs、transition oracle refs、domain handler command refs | generated product-entry/status/workbench manifest shell、operator loop shell、family product-entry manifest v2 assembly | OPL generated/default caller 消费同一 pack/action/stage refs；direct/hosted parity、owner receipt/typed blocker roundtrip、no-forbidden-write 关闭后删除或 tombstone MAG handwritten shell | `tests/product_entry_cases/test_manifest_and_status.py`、`tests/product_entry_cases/test_status_start_cases.py` |
| `src/med_autogrant/product_entry_parts/sidecar.py` | 312 | `already_thin_adapter` with `opl_framework_migration_candidate` retirement gate | `product sidecar export|dispatch`、OPL family provider bridge、sidecar tests | refs-only sidecar export direct entry、stage/memory/lifecycle receipt dispatch、closeout receipt dispatch | sidecar projection/read-model helpers 已拆入 `sidecar_projection.py`，shared constants 已拆入 `sidecar_contract.py`，dispatch orchestration 已拆入 `sidecar_dispatch.py`；主文件继续作为 MAG receipt / refs-only domain handler target，不能扩回 generic sidecar platform | package authority、grant memory accept/reject receipt、owner receipt、typed blocker、safe action metadata | generated sidecar wrapper、typed queue dispatch shell、generic lifecycle/memory/package transport envelope；autonomy controller wrapper 已退役 | OPL sidecar wrapper default 化、owner receipt roundtrip、direct/hosted no-regression、no-active compat alias | `tests/product_entry_cases/test_sidecar.py`、`make test-meta` |
| `src/med_autogrant/product_entry_parts/sidecar_dispatch.py` | 386 | `already_thin_adapter` with `opl_framework_migration_candidate` retirement gate | `product_entry_parts/sidecar.py` dispatch path、closeout sidecar tests | task dispatch、receipt refs、typed blocker wrapping 和 task JSON 读取 | 从 `sidecar.py` 拆出的 refs-only dispatch envelope，仍服务 MAG receipt/domain handler actions；长期 generic dispatch shell 应由 OPL typed queue/sidecar wrapper 持有 | owner receipt、typed blocker、grant memory/package/lifecycle receipt target refs | OPL typed sidecar dispatch shell、queue/lifecycle transport envelope | OPL sidecar wrapper default 化、owner receipt roundtrip、direct/hosted parity、no-active compat alias | `tests/product_entry_cases/test_sidecar.py`、`tests/product_entry_cases/test_closeout_sidecar.py` |
| `src/med_autogrant/product_entry_parts/sidecar_projection.py` | 127 | `opl_framework_migration_candidate` | `product_entry_parts/sidecar.py` export path | TODO/wakeup、autonomy controller command projection、user-loop attention queue 与 default executor / first skill export helpers | 承担 OPL queue/read-model/export projection shell，不写 grant truth、verdict、package 或 memory body | owner/action refs、safe command metadata、typed blocker return shape refs | OPL generated sidecar export/workbench projection shell、typed wakeup queue projection | OPL generated sidecar export/workbench becomes default caller; MAG export consumes same refs; focused parity tests and no-forbidden-write proof | `tests/product_entry_cases/test_sidecar.py` |
| `src/med_autogrant/product_entry_parts/sidecar_contract.py` | 17 | `already_thin_adapter` | sidecar export/dispatch constants | sidecar surface kind、adapter id、schema version、allowed dispatch actions | Small shared contract module prevents constant duplication while active caller remains MAG sidecar direct path | allowed MAG domain handler action list until OPL caller cutover | generated sidecar descriptor/action allowlist | Delete or tombstone only after no-active MAG sidecar direct caller and generated sidecar parity | `tests/product_entry_cases/test_sidecar.py` |
| `src/med_autogrant/product_entry_parts/consumer_thinning.py` | 387 | `already_thin_adapter` | manifest/sidecar/functional audit projection | MAG consumer-thinning contract envelope、external evidence request pack、bridge exit gate、minimal authority refs 与 followthrough classification | 2026-05-22 已把 OPL replacement expectations、functional harness coverage、conflict/observability projection、thin output guard 和 scaffold alignment 移入 `consumer_thinning_shell.py`；主文件低于 line budget 并从 repository hygiene legacy over-target baseline 移除。当前仍是 refs-only migration contract，不应扩写成 runtime owner | retained authority taxonomy、request refs、verdict authority refs、forbidden output boundary | OPL functional privatization audit projection shell、generated consumer-thinning contract shell | 保持 refs-only；路径漂移时刷新 source refs；不声明 OPL replacement production ready；OPL generated/default caller parity 后删除或 tombstone 非 authority shell | `make test-meta`、`tests/test_opl_family_contract_adoption.py`、`tests/product_entry_cases/test_sidecar.py` |
| `src/med_autogrant/product_entry_parts/consumer_thinning_shell.py` | 550 | `opl_framework_migration_candidate` | `consumer_thinning.build_mag_consumer_thinning_contract(...)` | OPL replacement expectations、functional harness coverage、conflict blocker projection、runtime observability consumption、thin output guard、standard scaffold alignment | 集中承载可由 OPL functional privatization audit / generated wrapper / App workbench shell 接管的 generic projection/guard；不生成 fundability、quality、export、package、memory 或 owner receipt authority | MAG authority refs and forbidden-output boundary only | OPL functional audit/read-model shell、generated wrapper output guard、App/operator projection shell | OPL replacement parity、active caller cutover、MAG receipt/typed blocker roundtrip、no-forbidden-write proof、tombstone/provenance refs | `make test-meta`、`tests/test_opl_family_contract_adoption.py`、`tests/product_entry_cases/test_sidecar.py` |
| `src/med_autogrant/product_entry_parts/consumer_thinning_audit.py` | 776 | `already_thin_adapter` | product-entry manifest、functional audit tests | 私有功能面审计与 no-resurrection policy；调用 split helper 组装 retired/tombstone surface 清单 | retired / tombstone legacy surface 清单已拆到 `consumer_thinning_audit_parts.py`；主 audit 是 read model，不持有 runtime/action authority | grant authority surface classification | OPL audit detail projection | 保持 no generic owner flags；新增 surface 必须有 class 和 authority boundary；OPL audit/default caller parity 前继续保留 MAG read-model direct path | `make test-meta` |
| `src/med_autogrant/product_entry_parts/consumer_thinning_audit_parts.py` | 161 | `already_thin_adapter` with `opl_framework_migration_candidate` retirement gate | `consumer_thinning_audit.py` | retired/tombstone legacy surface audit helper：runtime owner、local journal/attempt ledger、patch bridge、flat alias、repo-owned scheduler daemon exit gates | 从大型 audit 文件中拆出的 legacy proof/tombstone 清单；只生成审计条目，不执行 runtime、scheduler、queue、ledger 或 verdict mutation | legacy-retirement evidence refs、authority boundary taxonomy | OPL functional privatization audit/read-model detail projection、legacy tombstone catalog | OPL audit/detail projection default caller、source refs parity、no-resurrection guard 和 no-active MAG private platform caller 证据成立后删除或 history tombstone | `make test-meta`、`tests/test_opl_family_contract_adoption.py` |
| `src/med_autogrant/product_entry_parts/consumer_thinning_pack.py` | 606 | `already_thin_adapter` | pack compiler / manifest read projection | minimal authority function taxonomy、declarative grant pack compiler input、generated surface handoff、bridge exit gate | external evidence request pack 与 generated/default caller proof 已拆到 `consumer_thinning_pack_evidence.py`；主 pack 是 machine handoff/projection，不持有 runtime/action authority | authority refs and pack boundary、minimal authority taxonomy | OPL generated surface compiler/read model、pack compiler handoff | OPL compile/readiness proof；不写 production/default caller complete；authority taxonomy 不迁移 | `tests/test_opl_family_contract_adoption.py` |
| `src/med_autogrant/product_entry_parts/consumer_thinning_pack_evidence.py` | 305 | `opl_framework_migration_candidate` | `consumer_thinning_pack.py` re-export、`consumer_thinning.build_mag_consumer_thinning_contract(...)` | external evidence request pack、generated/hosted default caller proof、direct/hosted parity workorder、no-forbidden-write boundary | 集中承载 OPL generated/default caller evidence shell 和 App/production receipt request projection；它请求/验证 refs，不实现 runtime/workbench，不声明 evidence received | required request ids、authority boundary refs、direct domain handler refs | OPL external evidence request primitive、generated/default caller proof shell、App/operator receipt request projection | OPL generated/default caller production consumption、external evidence ledger receipt parity、owner receipt/typed blocker roundtrip、continuous no-forbidden-write proof 后删除或 tombstone MAG request shell | `tests/product_entry_cases/test_external_evidence_request_pack.py`、`tests/product_entry_cases/test_external_evidence_consumption_ledger.py`、`tests/test_opl_family_contract_adoption.py` |
| `src/med_autogrant/product_entry_parts/functional_closure.py` | 517 | `already_thin_adapter` with `opl_framework_migration_candidate` retirement gate | product-entry manifest、sidecar export、functional closure tests、OPL family contract adoption | manifest functional closure surface assembly：standard skeleton、owner receipt contract、lifecycle guarded apply proof、consumer thinning contract、ideal-state closure status | physical skeleton/source-layout scan 已拆到 `functional_closure_skeleton.py`；主入口继续编排 MAG receipt/lifecycle refs，不执行 OPL runtime 或 App workbench | owner receipt contract refs、lifecycle guarded apply refs、grant transition oracle refs、authority boundary refs | generated manifest closure shell、lifecycle/source-layout projection assembly | OPL generated/default caller parity、owner receipt/typed blocker roundtrip、no-forbidden-write proof 后继续收薄；owner receipt signer 不迁移 | `tests/product_entry_cases/test_functional_closure.py`、`tests/test_opl_family_contract_adoption.py` |
| `src/med_autogrant/product_entry_parts/functional_closure_skeleton.py` | 318 | `opl_framework_migration_candidate` | `functional_closure.build_manifest_functional_closure_surfaces(...)`、`opl_standard_pack.build_physical_skeleton_follow_through(...)` | physical skeleton follow-through、repo source root anchors、legacy default caller active-path scan、retired surface path status | 该 helper 是 source-layout/scaffold/provenance/read-model projection 壳，只证明 repo-source anchors 与 legacy active-path absence；不签 receipt、不执行 lifecycle mutation、不声明 production soak | declarative grant pack root refs、retired-surface provenance refs、domain owner handoff refs | OPL scaffold/source-layout primitive、generated active-path scan/read-model projection、legacy tombstone catalog | OPL scaffold/source-layout default primitive parity、active caller cutover、MAG receipt parity、no-new-compat-alias proof 后删除或 history tombstone | `tests/product_entry_cases/test_functional_closure.py`、`tests/test_opl_family_contract_adoption.py` |
| `src/med_autogrant/grant_autonomy_controller.py` | 171 | `already_thin_adapter` with `opl_framework_migration_candidate` retirement gate | autonomy controller command、sidecar guarded action、controller tests | request validation、start/resume resolution、controller-plan governance、loop shell call | 原 961 行大型控制面已拆薄；当前文件是 MAG direct handler 编排入口，仍因 active caller 暂留，不能写成长期 runtime owner | grant route truth refs、budget/blocker policy refs、typed blocker/owner receipt handoff refs、AI-first review handoff refs | generated controller entry shell、operator loop shell、attempt lifecycle caller | OPL generated/default caller、provider/attempt parity、owner receipt/typed blocker roundtrip、no scheduler owner claim、focused tests、no-forbidden-write | `tests/test_grant_autonomy_controller.py`、`tests/test_grant_governance_adapter.py` |
| `src/med_autogrant/grant_autonomy_loop_shell.py` | 457 | `opl_framework_migration_candidate` | `grant_autonomy_controller.py` | loop/scheduler-like control envelope：quality callback lifecycle、budget spending、mainline runner invocation、rollback/reselection flow 与 grant gate 顺序 | generic report / fail-closed / tranche-history envelope 已拆到 `grant_autonomy_loop_parts.py`；主 shell 仍是从 controller 拆出的 active loop migration input，长期应由 OPL provider/attempt lifecycle 或 generated operator loop 承担；当前没有迁移 grant verdict 或 package authority | grant route/budget/blocker policy refs、typed blocker return shape、AI-first review/export verdict refs | scheduler/watch shell、generic attempt lifecycle、retry/dead-letter、operator loop shell | OPL active/default caller 迁入、direct/hosted parity、owner receipt/typed blocker roundtrip、continuous no-forbidden-write、no-active compat alias | `tests/test_grant_autonomy_controller.py`、`tests/test_grant_governance_adapter.py` |
| `src/med_autogrant/grant_autonomy_loop_parts.py` | 143 | `opl_framework_migration_candidate` | `grant_autonomy_loop_shell.py` | controller report / fail-closed report / tranche-history append envelope | 该模块只封装 generic report/history envelope 与 shared report参数传递，不写 grant quality/export verdict、package authority 或 memory body；适合作为 OPL generated operator loop / attempt report primitive 的 migration input | report 中的 grant authority refs、typed blocker / owner receipt handoff refs | generic attempt report envelope、operator loop report assembly、tranche/history projection shell | OPL attempt/operator report primitive parity；MAG loop shell active caller cutover；owner receipt/typed blocker roundtrip；no-forbidden-write proof | `tests/test_grant_autonomy_controller.py`、`tests/test_grant_governance_adapter.py` |
| `src/med_autogrant/grant_autonomy_parts.py` | 883 | `domain_authority_retained` with split pressure | autonomy controller internals | grant-specific route/budget/helper policy | 大但主要是 grant-native route helper；不迁 OPL，但应避免继续承载 generic loop | grant controller helper, typed blocker policy | none except generic loop envelope | 只做 semantic split，不迁 authority | `tests/test_grant_autonomy_controller.py` |
| `src/med_autogrant/cli.py` | 620 | `opl_framework_migration_candidate` | direct MAG CLI、product/user-loop/status/sidecar commands | grouped CLI dispatch | CLI wrapper 长期由 OPL generated surface 承担；MAG 保留 direct handler target until cutover | direct domain entry、owner receipt reader、grant command target | generated CLI/MCP/product shell | generated caller parity、no old alias/facade active caller、direct path no-regression | `tests/product_entry_cases/test_cli_dispatch.py`、`scripts/verify.sh` |
| `src/med_autogrant/domain_runtime_parts/io.py` | 625 | `already_thin_adapter` | final package / workspace IO helpers | workspace/package IO helper | IO helper 是 domain handler support；不应成为 runtime substrate owner | package/source IO validation | generic workspace/source locator only | OPL locator parity before any shell migration; package authority remains MAG | `tests/test_final_package.py` |
| `src/med_autogrant/product_entry_parts/runtime_surfaces.py` | 679 | `already_thin_adapter` | manifest/status/sidecar runtime projection | OPL provider/runtime contract refs and runtime-readiness projection | 已是 refs-only runtime contract projection；不能声明 MAG runtime owner | runtime authority boundary refs | OPL runtime/readiness projection owner | keep `opl_provider_runtime_contract`; no provider-ready => grant-ready claim | product-entry status tests |
| `src/med_autogrant/product_entry_parts/runtime_registration.py` | 324 | `already_thin_adapter` with `opl_framework_migration_candidate` retirement gate | skill catalog domain projection、product-entry manifest、runtime registration tests | OPL stage runtime registration、executor adapter refs、native helper/index consumption refs、family lifecycle adapter refs | 本轮已去掉 MAG 对 OPL helper 语言、具体 backing helper id 和 proof role 的声明；当前只输出 refs-only input coverage、`opl_index_only` policy 和 authority boundary | domain entry target refs、resume/progress command refs、grant truth owner boundary | helper implementation selection、indexer registry、runtime/watch helper ownership、lifecycle/session indexing implementation | OPL helper/index registry 成为 default owner；MAG 只保留 consumed refs / authority boundary；direct/hosted parity 和 no-forbidden-write receipt 稳定后进一步收薄 | `tests/product_entry_cases/test_runtime_registration.py`、`tests/test_cli_validate_workspace_product_entry_cases.py`、`tests/test_schema_registry.py` |
| `src/med_autogrant/product_entry_parts/owner_receipts.py` | 744 | `domain_authority_retained` | owner receipt / production acceptance / sidecar closeout | MAG owner receipt materialization | owner receipt signer 必须留 MAG | owner receipt signing, typed blocker refs | OPL receipt ledger/index only | 不迁 signer；OPL only stores/queries refs | `tests/test_production_acceptance.py`、owner receipt tests |
| `src/med_autogrant/stage_control_plane.py` | 725 | `domain_authority_retained` | pack compiler, manifest, OPL stages readiness | MAG stage semantics and transition oracle | stage semantics 属于 grant domain pack；OPL 可消费但不能改写 verdict | stage prompt/skill/quality gate semantics, transition oracle | generic stage runner/attempt shell | OPL consumes stage refs; no quality verdict generation | stage control / OPL family contract tests |
| `src/med_autogrant/opl_standard_pack.py` | 736 | `domain_authority_retained` | pack/conformance tests | declarative grant pack contract materialization | domain pack/contract source，不是 generic runtime；private policy builder 已拆出以满足 repo line budget | grant semantic pack, required refs | OPL pack compiler only consumes | Keep pack source; split only if maintainability requires | `tests/test_opl_family_contract_adoption.py` |
| `src/med_autogrant/opl_standard_pack_private_policy.py` | 196 | `already_thin_adapter` | `opl_standard_pack.py` policy builder | private functional surface policy materialization | 从 standard pack 拆出的同语义 builder，只生成 audit policy，不持有 runtime/action authority | private authority taxonomy and no-resurrection policy | OPL audit/policy consumer only | 保持生成合同无漂移；不加入 runtime owner 字段 | `tests/test_opl_standard_pack.py`、`make test-meta` |

## Bad-smell flags

- Large hand-written CLI aggregator: `src/med_autogrant/cli.py`.
- Private sidecar/runtime-looking shell: `product_entry_parts/sidecar.py`, `runtime_surfaces.py`, `domain_runtime_parts/io.py`.
- Status/read-model mixed with runtime authority risk: `manifest_builder.py` and product status surfaces.
- Compatibility wrapper risk: grouped CLI and old local runtime/Hermes language must remain tombstone/provenance only.
- Descriptor/external receipt ready must not be written as grant-ready, fundability-ready, export-ready or production long-soak complete.
- Mechanical scorecard/schema/package existence must not replace AI-first grant review / export verdict.

## Immediate thinning items

1. Continue manifest shell migration from the new split boundary: `manifest_builder_parts/shell_assembly.py` is now the isolated generic product-entry shell/operator assembly input; final retirement still waits for OPL generated/default caller evidence and direct/hosted parity.
2. Keep `sidecar.py` dispatch as MAG receipt / refs-only domain handler target until OPL generated sidecar is default; do not add more generic dispatch actions. Current TODO/wakeup output now uses `opl_wakeup_contract`, targets `open_grant_user_loop` outside sidecar dispatch, retires autonomy-controller sidecar wrappers, and forbids Hermes 24h substrate / MAG scheduler / MAG attempt ledger / MAG App workbench ownership. Sidecar export projection helpers now live in `sidecar_projection.py`; this is a split boundary, not OPL sidecar default-caller completion.
3. Keep `grant_autonomy_controller.py` thin and do not grow it back into a loop owner; treat `grant_autonomy_loop_shell.py` plus `grant_autonomy_loop_parts.py` as the isolated OPL migration input for generic attempt lifecycle / operator loop report shell, while MAG retains route/budget/blocker policy and AI-first verdict handoff refs.
4. Keep `consumer_thinning.py` and `consumer_thinning_shell.py` as refs-only audit/projection inputs; do not add runtime owner fields. The shell split is a migration boundary, not OPL replacement/default-caller completion.
5. Keep `consumer_thinning_audit.py` and `consumer_thinning_audit_parts.py` as read-model / tombstone-audit inputs; do not add scheduler, queue, ledger, runtime owner, compatibility alias or grant-ready/export-ready claims to either file.
6. Keep `consumer_thinning_pack_evidence.py` as an external evidence request/default-caller proof shell only; do not let it become a MAG-owned OPL runtime, App workbench, production caller, or evidence ledger implementation.
7. Keep `functional_closure_skeleton.py` as source-layout/scaffold/provenance projection only; do not let active-path scan, root anchors, or descriptor follow-through become grant readiness, export readiness, owner receipt signing, or production-soak evidence.

## OPL primitive dependencies

- generated product/status/workbench/sidecar default caller;
- typed queue and sidecar dispatch shell with owner receipt roundtrip;
- OPL wakeup/provider queue shell consuming `open_grant_user_loop` refs, with MAG returning only owner receipt / typed blocker / no-regression refs;
- generic lifecycle/session/package locator shell;
- App/operator read model consuming MAG refs without grant verdict generation;
- production/default caller and Temporal long-soak evidence from OPL/App side.

## Forbidden claims

- `mag_functional_structure_gap_count=0` does not mean physical source cleanup complete.
- OPL stage evidence receipt verified does not mean grant-ready or submission-ready.
- package existence, quality scorecard, schema completeness, controller route or queue completion cannot become fundability/quality/export verdict.
- active caller still in MAG means OPL has not fully taken over that surface.
