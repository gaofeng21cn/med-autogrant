# Med Auto Grant Agent Boundary

Owner: Med Auto Grant
Purpose: repo-source anchor for the OPL standard domain-agent skeleton.
State: descriptor/index surface only.
Machine boundary: active executable truth remains in `src/med_autogrant/`, `contracts/`, and product-entry manifests.

This directory exists to make the standard `agent/`, `contracts/`, `runtime/`, `docs/` skeleton physically auditable. It does not store grant artifacts, receipt instances, memory bodies, fundability verdicts, authoring quality verdicts, or submission-ready export verdicts.
