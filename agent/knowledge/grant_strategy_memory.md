# Grant Strategy Memory Knowledge

Grant strategy memory content is MAG-owned and workspace/runtime-state backed. Repo source carries descriptor refs, policy refs, locator templates, consumed-memory proof refs, writeback proposal refs, and receipt refs only.

Accept/reject decisions must preserve funding-call lock, applicant evidence context, reviewer risk, and quality closure semantics. OPL can index and route refs, but cannot store memory body or accept/reject MAG memory writeback.
