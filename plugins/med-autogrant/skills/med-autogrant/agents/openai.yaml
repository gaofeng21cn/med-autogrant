interface:
  display_name: "Med Auto Grant"
  short_description: "Grant authoring domain agent for Codex"
  default_prompt: "Use $med-autogrant to inspect the current grant workspace and continue through MAG's grant-native handlers and authority targets."
