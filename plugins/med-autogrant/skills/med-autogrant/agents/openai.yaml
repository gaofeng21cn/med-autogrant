interface:
  display_name: "Med Auto Grant"
  short_description: "Medical grant planning, writing, review, and revision"
  default_prompt: "Use $med-autogrant through the installed OPL-generated MAG actions to continue the current funding-call workflow, a bounded proposal-authoring task, or an explicitly requested local submission package."
